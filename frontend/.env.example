# Copy to .env.local and edit if the API runs elsewhere.
# Only the FastAPI address goes here. VITE_ variables are bundled into the public JavaScript,
# so never put secrets (such as the Sarvam API key) in this file. The key stays on the server in ../.env.
VITE_API_BASE_URL=http://127.0.0.1:8000
